# Safe dummy values for the test suite (vitest/supertest). Committed on purpose.
PORT=0
BASE_URL=http://localhost:3000
SESSION_TTL_MS=60000
MDL_CLAIMS=family_name,given_name
TRUSTED_ISSUERS_DIR=./trusted-issuers
